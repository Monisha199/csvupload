Create a folder called "csvUpload"
Create index.js in it
Run "npm init"
Create required folders like models etc
Run npm install express
Run npm install ejs
Create home.ejs in views
Create controller for it home.ejs
Run npm install Mongoose 
Create config and add mongoose config there
